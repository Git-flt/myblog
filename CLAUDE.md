# CLAUDE.md

本文件记录 myblog 的**定调与约束**，供后续会话遵循。操作细节见 `README.md`，完整方案见 `/opt/projects/AI-Native改造方案.md`（v4）。

---

## 一、定位

**个人技术写作站，文字为主、作品为辅。**

- 一句话定位：**从运维视角看 AI 系统**
- 受众：同行与招聘方，不是客户。**当前不考虑盈利**
- 长期目标：支撑作者向 **AI Platform Engineer / AI Platform Architect** 方向发展
- 作者背景：一线城市 IT 20 余年，本职 AIOps / 大数据

判断任何改动是否该做，先问：**它是否让「这个人对 AI 系统有成体系的、来自生产一线的思考」更容易被看见？**

---

## 二、内容红线（最重要，不可妥协）

| AI 可以做 | 必须人工 |
|-----------|---------|
| 资料检索与初步整理 | **观点与判断** |
| 结构建议、语言润色 | **事实核查**（尤其数字、版本、产品能力） |
| 摘要 / tags / excerpt / 配图 | **个人经验与案例** |
| 代码示例初稿 | **结论与推荐** |

**硬规则：不发布任何未经作者亲自验证的技术方案。** 纯推演内容必须在文中明确标注。

这条已公开写在 `/about` 页面上。作品站一旦被识别为 AI 水文，可信度归零——这个定位下，可信度就是全部。

**因此：不要代写文章正文，不要生成"看起来专业"的对比表格或选型结论。** 需要写作辅助时，产出的是结构建议、待核查清单、素材整理，不是成稿观点。

---

## 三、选题标准

**更新节奏：每月至少 2 篇**（作者承诺，已公开写在站点上）。

**判断标准：如果这篇文章 ChatGPT 也能写出八成，就不要写。**

作者唯一无法被复制的资产是 20 余年生产运维经验。选题应落在这个交叉带：

- ✅ Agent 系统的可观测性该监控什么、LLM 作为依赖项的超时/重试/降级/熔断、RAG 的容量规划与成本模型、20 年运维经验在 agent 时代的复用与失效
- ⚠️ 泛化的「AI 前沿汇总」「某产品发布解读」——红海，AI 最容易批量生成，只在有独立判断时写

有一个高价值的可持续系列：**「AI 出的架构方案，我去验证它」**——让 AI 出方案 → 亲自跑通 → 写对了什么、错了什么、漏了什么。评估 AI 给出的架构方案本身就是 2026 年架构师的核心技能。首篇素材是 `/opt/projects/public/AI-Agent记忆层选型与落地方案.md`（**注意：该文档系 AI 生成且从未执行，不可作为作者产出直接发布**）。

---

## 四、内容组织

**按主题聚类，不按时间流。** 时间流让好文章随时间沉底；主题聚类让它持续被读到、被 AI 引用。

| topic | 名称 | 定位 |
|-------|------|------|
| `aiops-x-ai` | 运维视角看 AI 系统 | **主线** |
| `architecture` | 架构分析与验证 | **主线** |
| `trends` | 趋势观察 | 辅助，只写有独立判断的 |
| `notes` | 学习笔记 | 归档，不再新增 |

两条主线目前为空，位置已留好。新增主题需同步改 `src/content.config.ts` 与 `src/config/site.ts`。

---

## 五、技术约束

- **URL 不可变**：`/articles/<slug>.html` 是从旧管道继承的基线，变更会使外链与既有收录失效。`scripts/verify-build.mjs` 在 CI 中比对，不要绕过
- **栈已定**：Astro 5。托管当前在 GitHub Pages，**目标平台为 Vercel**（`vercel.json` 已就绪，零环境变量即可部署）。**不引入新东西**
- **平台无关**：`SITE_URL` / `BASE_PATH` 两个环境变量决定站点地址，代码里不要硬编码域名或子路径；内部链接一律走 `src/lib/posts.ts` 的 `url()`
- **明确不做**（当前阶段）：
  - ❌ Supabase / 向量库 —— 文字站现阶段不需要
  - ⏸ RAG 问答 `/api/ask` —— 文章少于 30 篇时价值有限，Pagefind 够用
  - ⏸ 站点 MCP server —— 作品模块出现后再做
  - ❌ 可观测性面板 —— 文字为主的站上属于炫技
- **作品展示**：暂以列表形式放在 `/about`。等作品积累到值得深挖时再拆独立 `/works`，届时用四段式（问题 → 决策与权衡 → 系统证据 → 结果复盘）
- 视觉：复古纸感主题，CSS 沿用不重设计

---

## 六、待作者决定

1. **域名与对外署名** —— 现为 `git-flt.github.io/myblog`。独立域名是 GEO 权重积累的前提
2. **何时切到 Vercel** —— 配置已就绪。切换会使既有 `github.io/myblog/...` 链接失效，届时应保留 GitHub Pages 部署作为跳转层
3. **openclaw 一文的日期** —— 迁移时按文章自身署名填为 `2026-01-30`（旧站显示 `2026-03-08`，因原 frontmatter 缺 date 而由文件时间兜底）

---

## 七、协作方式

- **范围严格贴住当次指定的项目**，不要主动扩展到其他仓库或提出跨项目重构
- **下判断前先核实事实**，尤其是资产归属：`/opt/projects/public/` 是混放的收集目录，其中内容不默认属于作者（例如 `neudrive` 属于 `agi-bar/`，是参考的别人的项目）
- 作者的其他仓库：`Git-flt/` 下的 **BeijingCIMS**（真实上线，最强作品）、**WorkStudio**（保持现状不动）、**MyTimeMachine**（已冻结）
