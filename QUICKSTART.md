# 🚀 MyBlog v2.0 快速开始指南

## 📦 前置要求

- Node.js 14+ 
- npm 6+
- Git（可选，用于版本控制）

---

## ⚡ 5分钟快速上手

### 步骤 1：安装依赖

```bash
cd myblog
npm install
```

预期输出：
```
added 15 packages in 8s
```

---

### 步骤 2：发布你的第一篇文章

在 `drafts/` 目录创建 `my-article.md`：

```markdown
---
title: 我的第一篇文章
date: 2026-01-28
tags: [技术, 博客]
excerpt: 这是我的第一篇博客文章
---

# 欢迎

这是我的第一篇博客文章！

## 代码示例

\`\`\`javascript
console.log('Hello, World!');
\`\`\`

## 结论

开始写博客真简单！
```

---

### 步骤 3：构建并预览

```bash
# 发布文章 + 生成 RSS + 构建搜索
npm run build

# 启动本地服务器
npm run serve
```

打开浏览器访问：**http://localhost:8080**

---

## ✨ 新功能体验指南

### 🔍 全文搜索
1. 在首页顶部看到搜索框
2. 输入关键词（如 "技术"）
3. 即时显示搜索结果

### 🏷️ 标签筛选
1. 在文章列表上方看到标签按钮
2. 点击任意标签（如 "技术"）
3. 文章列表自动筛选

### 📡 RSS 订阅
1. 点击导航栏的 📡 图标
2. 或访问 `/feed.xml`
3. 在 RSS 阅读器中添加订阅

### 📋 代码复制
1. 打开任意文章
2. 找到代码块
3. 悬停看到 "📋 复制" 按钮
4. 点击一键复制代码

### ⏱️ 阅读时长
- 每篇文章标题下显示预计阅读时间
- 例如：⏱️ 约 5 分钟阅读

---

## 🔧 配置清单

### 必须配置

#### 1. 修改 RSS 域名
编辑 `publish.js` 第 194 行：

```javascript
const siteUrl = 'https://yourdomain.com';  // 改成你的域名
const siteTitle = 'My Blog';                // 改成你的博客名
```

#### 2. 安装依赖
```bash
npm install
```

### 可选配置

#### 3. 启用 Giscus 评论
查看详细指南：`GISCUS_SETUP.md`

简要步骤：
1. 创建 GitHub 公开仓库
2. 启用 Discussions
3. 访问 https://giscus.app/zh-CN 获取配置
4. 更新 `giscus-config.js`

---

## 📝 日常写作流程

### 1. 创建文章

在 `drafts/` 创建 `your-article.md`：

```markdown
---
title: 文章标题
date: 2026-01-28
tags: [标签1, 标签2]
excerpt: 文章摘要（显示在首页）
slug: custom-url  # 可选，自定义 URL
---

文章内容...
```

### 2. 发布

```bash
npm run build
```

这会自动：
- ✅ 转换 Markdown → HTML
- ✅ 生成 RSS feed
- ✅ 更新搜索索引
- ✅ 更新首页文章列表

### 3. 预览

```bash
npm run serve
# 访问 http://localhost:8080
```

### 4. 部署

```bash
# 使用内置部署脚本
./deploy.sh

# 或手动上传
# 上传整个 myblog/ 目录到服务器
```

---

## 🛠️ 常用命令

```bash
# 发布文章
npm run publish

# 构建搜索索引
npm run build:search

# 完整构建（推荐）
npm run build

# 本地预览
npm run serve

# 代码质量检查
npm run lint

# 自动修复代码问题
npm run lint:fix

# 格式化代码
npm run format

# 运行测试
npm test
```

---

## 📁 项目结构

```
myblog/
├── drafts/              # Markdown 源文件（你在这里写文章）
│   ├── my-article.md
│   └── 文章模板.md
├── articles/            # 生成的 HTML 文章（自动生成）
│   └── my-article.html
├── css/                 # 样式文件
│   └── style.css
├── js/                  # JavaScript 脚本
│   ├── search.js        # 搜索功能
│   └── tag-filter.js    # 标签筛选
├── index.html           # 首页
├── feed.xml             # RSS feed（自动生成）
├── publish.js           # 发布脚本
├── server.js            # 开发服务器
├── package.json         # 项目配置
└── README.md            # 项目文档
```

---

## ❓ 常见问题

### Q: 搜索功能不工作？
**A**: 搜索需要 HTTP 服务器环境：
```bash
npm run serve  # 启动本地服务器
```

### Q: 文章没有更新？
**A**: 重新运行构建命令：
```bash
npm run build
```

### Q: RSS feed 链接错误？
**A**: 修改 `publish.js` 第 194 行的域名配置

### Q: 如何修改样式？
**A**: 编辑 `css/style.css`，修改 CSS 变量：
```css
:root {
    --primary-color: #2c3e50;  /* 主色调 */
    --secondary-color: #3498db; /* 强调色 */
}
```

### Q: 如何添加自定义页面？
**A**: 在根目录创建 HTML 文件，样式会自动继承

---

## 🎯 推荐工作流

### 每日写作
```bash
# 1. 在 drafts/ 写文章
# 2. 发布
npm run build
# 3. 预览
npm run serve
```

### 每周维护
```bash
# 1. 检查代码质量
npm run lint
# 2. 格式化代码
npm run format
# 3. 提交到 Git
git add .
git commit -m "Add new articles"
```

### 每月优化
- 审查文章标签是否合理
- 检查 RSS feed 是否正常
- 测试搜索功能效果
- 查看 Giscus 评论

---

## 📚 更多资源

- **完整文档**：`README.md`
- **更新日志**：`CHANGELOG.md`
- **升级报告**：`UPGRADE_REPORT.md`
- **Giscus 指南**：`GISCUS_SETUP.md`

---

## 🎉 开始你的博客之旅！

现在你已经掌握了所有必要的知识，开始写作吧！

```bash
# 创建你的第一篇文章
cd drafts
# 编辑文件...

# 发布
cd ..
npm run build

# 启动服务器
npm run serve

# 打开浏览器
# http://localhost:8080
```

祝你写作愉快！✍️
